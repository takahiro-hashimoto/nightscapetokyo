{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // \"\"'); echo \"$f\" | grep -q 'non-critical\\.css$' && npm --prefix /Users/takahiro/Desktop/nigitscape-next run css:minify 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
